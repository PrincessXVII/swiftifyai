import type { AIModel } from '../types';

export const AI_MODELS: AIModel[] = [
  {
    id: 'swiftify-core',
    name: 'Swiftify Core',
    provider: 'internal',
    available: true,
    description: 'Основная модель Swiftify',
  },
];
